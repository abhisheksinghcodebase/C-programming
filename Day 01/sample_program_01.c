// This is a sample program that prints a simple message to the console.


#include <stdio.h> // Input/output library for C programming language.

int main(){ // The main function is the entry point of the program. It is where the execution of the program begins.
    scanf("%*c"); // Wait for the user to press the Enter key before proceeding. This is done to keep the console window open until the user is ready to close it.
    printf("hello, world!"); // Print the message "hello, world!" to the console.

    return 0; // Return 0 to indicate that the program has executed successfully.
}